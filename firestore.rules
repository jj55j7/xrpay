rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow ALL authenticated users to do ANYTHING (for development)
    // WARNING: This is completely open - DO NOT use in production!
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}